# 个人主页使用说明

## 关于本网站

本网站是西安交通大学魏笔凡研究员的个人学术主页，基于纯静态 HTML + CSS 构建，部署在 **GitHub Pages** 上。

- 网站地址：https://weibifan.github.io/
- 源代码仓库：https://github.com/weibifan/weibifan.github.io
- 本地文件位置：`d:\MyCodeSpace\MyGithub\homepage_repo\`

---

## 目录结构

```
homepage_repo/
├── index.html              # 主页面（所有内容都在这里）
├── README.md               # 项目说明
├── assets/
│   └── css/
│       └── main.css        # 样式文件
└── images/
    └── profile.jpg         # 个人照片
```

> **核心文件只有一个：`index.html`**，所有页面内容、布局都在这个文件中。

---

## 如何修改内容

### 1. 增加一篇论文

打开 `index.html`，找到 `<section id="publications">` 部分（即"代表性论文"区域），在 `<ol class="pub-list pub-numbered">` 列表中添加新的 `<li>` 条目：

```html
<li>
  <span class="pub-authors">作者1, 作者2, <strong>Bifan Wei</strong>, 作者4.</span>
  <span class="pub-title">论文标题.</span>
  <span class="pub-venue"><em>期刊/会议名称</em>, 卷(期), 页码, 年份. (CCF X类)</span>
</li>
```

**示例**（添加到末尾，调整编号会自动处理）：

```html
<li>
  <span class="pub-authors">张三, <strong>Bifan Wei</strong>, 李四.</span>
  <span class="pub-title">面向知识图谱的可解释推理方法研究.</span>
  <span class="pub-venue"><em>计算机学报</em>, Vol.48(3), pp.1-20, 2027. (CCF A类中文期刊)</span>
</li>
```

### 2. 增加一个科研项目

找到 `<section id="research">` 部分，在对应的 `<ul class="pub-list">` 中添加：

```html
<li>
  <span class="project-date">2026.01~2028.12</span>
  <strong>项目类型</strong> — 项目名称（项目编号）
</li>
```

### 3. 增加一条新闻

找到 `<section id="news">` 部分，在 `.timeline` 中添加：

```html
<div class="timeline-item">
  <span class="timeline-date">2026</span>
  <span class="timeline-text">获奖/新闻内容</span>
</div>
```

### 4. 修改个人简介

找到 `<section id="about-me">` 部分，修改 `<p>` 标签中的文字内容。

### 5. 修改个人照片

将新图片命名为 `profile.jpg`，替换 `images/profile.jpg` 文件即可。建议尺寸：**正方形，至少 300×300 像素**。

---

## 如何预览修改

直接用浏览器打开 `index.html` 即可查看效果，无需任何服务器。

```bash
# 在项目目录下直接打开
start d:\MyCodeSpace\MyGithub\homepage_repo\index.html
```

---

## 如何发布到线上

修改完成后，执行以下命令推送到 GitHub 即可自动部署：

```bash
cd d:\MyCodeSpace\MyGithub\homepage_repo

git add .
git commit -m "更新内容：增加了XXX"
git push
```

等待约 **1-2 分钟**，访问 https://weibifan.github.io/ 即可看到更新。

> **注意**：第一次推送需要配置 Git 代理，后续操作不需要重新配置。
> ```bash
> git config --global http.proxy http://127.0.0.1:2802
> git config --global https.proxy http://127.0.0.1:2802
> ```

---

## GitHub Token 使用说明

如果你需要让 AI 助手（如 Claude Code）帮你更新主页，需要提供一个 GitHub Personal Access Token：

1. 打开 https://github.com/settings/tokens?type=beta
2. 点击 **Generate new token** → **Fine-grained token**
3. Token name: `claude-code-homepage`
4. **Repository access** → **Only select repositories** → 选中 `weibifan/weibifan.github.io`
5. **Permissions** → **Contents: Read and write**
6. 生成后将 token 发给 AI 助手

---

## 设计参考

本主页样式参考了 [academicpages](https://github.com/academicpages/academicpages.github.io) 学术主题，采用简洁、清晰的设计风格，适配桌面和移动端。

---

## 常见问题

**Q: 网站更新后没有变化？**
A: GitHub Pages 部署需要约 1-2 分钟，请耐心等待后刷新页面。也可以在仓库的 Actions 标签页查看部署进度。

**Q: 想在主页添加新的板块？**
A: 在 `index.html` 中复制一个已有的 `<section>` 块，修改标题和内容即可；同时需要在顶部导航栏 `<ul class="nav-links">` 中添加对应的菜单项。

**Q: 手机上看排版不对？**
A: 页面已支持响应式布局。如果仍有问题，请告诉我具体现象，我来调整 CSS。
